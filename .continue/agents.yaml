version: 1

agents:
  - name: "ShadowFixer"
    description: "Autonomous repo fixer bot"
    rules:
      - ".continue/rules.md"
    commands:
      - "npm install"
      - "npm run build"
      - "npm test"
